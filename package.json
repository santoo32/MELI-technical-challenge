

{
    "name": "mltechnicalchallenge",
    "version": "0.3.0",
    "scripts": {
      "postinstall": "npm install --prefix api && yarn install --cwd webapp"
    },
    "engines": {
      "node": "10.x",
      "yarn": "1.x"
    }
  }
  
  